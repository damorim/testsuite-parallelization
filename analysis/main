#!/bin/bash
if [[ ! -d "out" ]]; then
    mkdir out
fi

# Subject selection
./selection.py && R --vanilla --quite < selection.r --args "gen/selection.csv"

# FIXME consider the other executions as well...
# RQ 1
R --vanilla --quite < timecost.r --args "data/execution-1.csv"


# RQ 2
R --vanilla --quite < testcost.r --args "testcost-med" 
R --vanilla --quite < testcost.r --args "testcost-long" 

